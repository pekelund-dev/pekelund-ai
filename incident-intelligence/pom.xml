<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Parent POM for the IT Incident Intelligence Platform (ITII).

        This is a multi-module Maven project demonstrating how to build a
        production-grade AI-powered system using:
          - Spring Boot 3.x
          - Spring AI 1.1.0 (ChatClient, Tool annotations, MCP support)
          - Model Context Protocol (MCP) for tool exposure / consumption
          - PostgreSQL for persistence (via Flyway migrations)

        Note on "best practices" (and yes, that IS the correct spelling!):
          - Each module has a single responsibility
          - Configuration is externalised via environment variables
          - All secrets stay out of source control
          - Docker Compose handles orchestration
    -->

    <groupId>dev.pekelund</groupId>
    <artifactId>pekelund-ai</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>IT Incident Intelligence Platform</name>
    <description>
        AI-powered incident response system using Spring AI Agents and MCP (Model Context Protocol).
        Two modules: an MCP Server that exposes domain tools, and an Agent App that uses those
        tools to triage, diagnose and generate post-mortems for IT incidents.
    </description>

    <modules>
        <module>incident-mcp-server</module>
        <module>incident-agent-app</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dependency versions -->
        <spring-boot.version>4.0.3</spring-boot.version>
        <spring-ai.version>2.0.0-M2</spring-ai.version>
        <spring-ai-a2a.version>0.2.0</spring-ai-a2a.version>
        <lombok.version>1.18.36</lombok.version>
        <flyway.version>10.20.1</flyway.version>
        <postgresql.version>42.7.4</postgresql.version>
        <testcontainers.version>1.20.4</testcontainers.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring AI BOM — provides version management for all Spring AI artifacts -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
                MCP starters are part of the Spring AI BOM from 1.1.x onwards.
                These are listed here explicitly so child modules don't need versions.
            -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
                <version>${spring-ai.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-starter-mcp-client</artifactId>
                <version>${spring-ai.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-starter-model-google-genai</artifactId>
                <version>${spring-ai.version}</version>
            </dependency>

            <!--
                Spring AI Community A2A Server autoconfigure.
                Provides AgentCardController, MessageController, TaskController, and
                all supporting A2A infrastructure (TaskStore, QueueManager, etc.)
                when an AgentCard + AgentExecutor bean are present.
                Requires Spring Boot 4.0.x and Spring AI 2.0.0-M2+.
            -->
            <dependency>
                <groupId>org.springaicommunity</groupId>
                <artifactId>spring-ai-a2a-server-autoconfigure</artifactId>
                <version>${spring-ai-a2a.version}</version>
            </dependency>

            <!-- Testcontainers BOM -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- Spring Milestone repository required for Spring AI releases -->
    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
