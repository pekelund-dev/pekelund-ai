-- ============================================================
-- Agent App Flyway migration — this file is intentionally empty.
-- The full schema is created by the MCP server (V1__create_tables.sql).
-- The agent-app's Flyway baseline ensures its own flyway_schema_history_agent
-- table is created, but no DDL changes are made here.
-- ============================================================
-- This placeholder satisfies Flyway's requirement of at least one migration
-- file when flyway.baseline-on-migrate=true.

-- Future agent-app-specific schema additions can be added as V2, V3, etc.
